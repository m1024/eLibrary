@model  IEnumerable<eLibrary.Models.Genre>

<ul class="list-unstyled">
    @foreach (var item in Model)
    {
        <li>
                @Html.ActionLink(@item.Name, "Books", "Genres", new { id = item.Id }, new { @class = "btn btn-default genre-list" })
        </li>
    }
</ul>